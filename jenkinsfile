#!groovy

@Library('jenkinslib') _

def tools = new org.devops.tools()

pipeline {
    stages {
        stage('Hello') {
            steps {
                //echo 'Hello World'
                srcipt {
                    tools.PrintMes("library test")
                }
            }
        }
    }
}
